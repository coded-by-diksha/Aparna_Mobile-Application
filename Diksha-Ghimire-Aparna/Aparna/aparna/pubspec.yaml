name: aparna
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  slider_captcha: ^1.0.2
  http: ^1.5.0
  json: ^0.20.4
  sliding_action_button: ^0.0.8
  table_calendar: ^3.2.0
  fl_chart: ^0.69.0
  flutter_bloc: ^8.1.6
  # For value equality in BLoC states/events
  equatable: ^2.0.5
  healthicons_flutter: ^3.5.0
  flutter_svg: ^2.0.10+1
  flutter_localizations:
    sdk: flutter
  intl: 0.20.2
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^11.0.0 # For geolocation services
  geocoding: ^3.0.0   # For reverse geocoding (address lookup)
  flutter_map_cancellable_tile_provider: ^3.0.2 # For cancellable tile loading
  flutter_markdown: ^0.7.3 # For rendering markdown content
  firebase_core: ^3.8.1  # For initializing Firebase
  firebase_messaging: ^15.1.5 # For push notifications
  flutter_local_notifications: ^18.0.1 # For local notifications
  health: ^13.3.1 # For health data integration
  permission_handler: ^12.0.1
  flutter_blue_plus: ^1.32.7 #For Bluetooth functionality
  speech_to_text: ^7.3.0
  google_sign_in: ^7.2.0
  flutter_secure_storage: ^10.0.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

# App launcher icon (replaces default Flutter icon with aparna_logo.png)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/aparna_logo.png"
  adaptive_icon_foreground: "assets/aparna_logo.png"
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  assets:
    - .env
    - assets/aparna.png
    - assets/aparna_logo.png
    - assets/captcha_img.png
    - assets/welcome.png
    - assets/icons/
    - assets/aama.png
    - assets/expert.png
    - assets/users.png
    - assets/blog_page.png
  
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
